node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Netlify CLI
.netlify/

# Leftovers from the old on-disk SQLite database. Orders live in Neon now;
# these entries stay so the stale files can never be committed.
data/*.db
data/*.db-shm
data/*.db-wal

# Secrets
.env
.env.local
.env*.local

.DS_Store
.neon
